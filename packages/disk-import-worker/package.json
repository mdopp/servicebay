{
  "name": "@servicebay/disk-import-worker",
  "version": "0.0.0",
  "private": true,
  "description": "Resource-capped one-shot disk-import worker: the deterministic walk/hash/classify/dedup/plan engine plus a CLI entrypoint and the compact status-file contract shared with servicebay. Foundational slice of #1949 (heavy jobs run in their own capped container, not the control plane).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./contract": "./src/contract/status.ts"
  },
  "bin": {
    "disk-import-worker": "src/cli/main.ts"
  },
  "scripts": {
    "worker": "tsx src/cli/main.ts"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.1"
  }
}
