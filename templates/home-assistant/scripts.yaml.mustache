# Home Assistant UI-editable scripts. Loaded via
# `script: !include scripts.yaml` in configuration.yaml. HA's script
# editor rewrites this file; ServiceBay only seeds it empty on first
# install so the include never points at a missing file. A
# backup-restore replaces this with your real scripts.
{}
