# Home Assistant UI-editable scenes. Loaded via
# `scene: !include scenes.yaml` in configuration.yaml. HA's scene
# editor rewrites this file; ServiceBay only seeds it empty on first
# install so the include never points at a missing file. A
# backup-restore replaces this with your real scenes.
[]
