# Home Assistant UI-editable automations. Loaded via
# `automation: !include automations.yaml` in configuration.yaml.
# HA's automation editor rewrites this file; ServiceBay only seeds it
# empty on first install so the include never points at a missing file.
# A backup-restore replaces this with your real automations.
[]
