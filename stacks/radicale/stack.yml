apiVersion: v1
kind: Stack
metadata:
  name: radicale
  annotations:
    servicebay.label: "Radicale (CalDAV + CardDAV)"
    servicebay.tier: "feature"
    servicebay.depends-on-stacks: "basic"
spec:
  templates: [radicale]
