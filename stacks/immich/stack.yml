apiVersion: v1
kind: Stack
metadata:
  name: immich
  annotations:
    servicebay.label: "Immich (Photos + AI search)"
    servicebay.tier: "feature"
    servicebay.depends-on-stacks: "basic"
spec:
  templates: [immich]
